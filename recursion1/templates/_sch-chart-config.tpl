{{- /*
"recursion.sch.chart.config.values" contains the chart specific values used to override or provide
additional configuration values used by the Shared Configurable Helpers.
*/ -}}
{{- define "recursion.assistant.sch.chart.config.values" -}}
sch:
  chart:
    appName: "recursion"
{{- end -}}
